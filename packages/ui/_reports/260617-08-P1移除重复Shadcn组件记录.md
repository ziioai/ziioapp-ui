# P1 移除重复 Shadcn 组件记录

## 完成内容

- 已删除 `apps/base/src/components/ui` 下 55 个重复 shadcn/Base UI 组件。
- `packages/ui/src/components` 保留为唯一 shadcn/Base UI 组件来源。
- app 侧继续通过 `@ziioapp/ui/components/*` 和 `@ziioapp/ui/lib/utils` 引用基础 UI。

## 验证

- 删除前扫描未发现 `@/components/ui` 等旧引用。
- 删除后 `apps/base/src/components/ui` 已不存在。
- `pnpm typecheck` 通过。
- `pnpm build` 通过，SSG 预渲染 `/`、`/about`、`/settings`、`/opfs-finder`。
- `pnpm build:electron` 通过。

## 备注

这一步把 UI 包拆分从“半迁移”收口到更合理的 monorepo 形态：基础 UI 归 `packages/ui`，app 只保留 layout/page/view/finder 等应用层组件。
